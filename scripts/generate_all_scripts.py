import os\nimport requests\n\nAPI_KEY = os.getenv('CLAUDE_API_KEY')\nAPI_URL = 'https://api.claude.ai/generate'\n\nSCRIPTS = [\n    'find_trending_videos.py',\n    'download_video.py',\n    'analyze_with_claude.py',\n    'detect_scenes.py',\n    'create_clips.py',\n    'upload_tiktok.py',\n    'upload_instagram.py',\n    'upload_youtube.py',\n    'main_bot.py'\n]\n\ndef generate_script(script_name):\n    response = requests.post(API_URL, json={'script_name': script_name, 'api_key': API_KEY})\n    if response.status_code == 200:\n        return response.text\n    else:\n        raise Exception(f'Error generating {script_name}: {response.status_code} - {response.text}')\n\n\ndef main():\n    for script in SCRIPTS:\n        try:\n            code = generate_script(script)\n            with open(os.path.join('scripts', script), 'w') as f:\n                f.write(code)\n            print(f'Successfully generated {script}')\n        except Exception as e:\n            print(str(e))\n\nif __name__ == '__main__':\n    main()\n